[project]
name = "aios-sdk-python"
version = "0.1.0"
description = "AIOS Python SDK for agent development"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

