{
	"name": "aios",
	"version": "0.1.0",
	"description": "AIOS — AI-Native Operating System. The OS layer for agent-first computing.",
	"type": "module",
	"private": true,
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev",
		"lint": "turbo run lint",
		"format": "biome check --write .",
		"typecheck": "turbo run typecheck",
		"clean": "turbo run clean && rm -rf node_modules",
		"test": "turbo run test"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@types/node": "^22.10.2",
		"turbo": "^2.3.3",
		"typescript": "^5.7.2"
	},
	"packageManager": "pnpm@9.15.1",
	"engines": {
		"node": ">=20.0.0",
		"pnpm": ">=9.0.0"
	}
}

