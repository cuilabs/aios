[package]
name = "aios-kernel-ipc"
version = "0.1.0"
edition = "2021"

[dependencies]
aios-kernel-net = { path = "../kernel-net" }
aios-kernel-events = { path = "../kernel-events" }
spin = "0.10"
bitflags = "2.5"

[features]
default = []
alloc = []

