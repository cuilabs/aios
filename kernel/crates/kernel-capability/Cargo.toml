[package]
name = "aios-kernel-capability"
version = "0.1.0"
edition = "2021"

[dependencies]
aios-kernel-events = { path = "../kernel-events" }
spin = "0.9"
bitflags = "2.5"

[features]
default = []
alloc = []
