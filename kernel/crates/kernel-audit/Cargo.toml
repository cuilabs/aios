[package]
name = "aios-kernel-audit"
version = "0.1.0"
edition = "2021"

[dependencies]
aios-kernel-core = { path = "../kernel-core" }
aios-kernel-crypto = { path = "../kernel-crypto" }
aios-kernel-events = { path = "../kernel-events" }
spin = "0.10"

[features]
default = []
alloc = []

