[package]
name = "aios-bootloader"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
x86_64 = { version = "0.14", features = ["instructions"] }
# UEFI support would go here
# multiboot2 = "0.11"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"

