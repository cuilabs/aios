{
	"name": "@aios/services/networkd",
	"version": "0.1.0",
	"type": "module",
	"description": "Network Daemon for AIOS - Network configuration, DHCP, and DNS",
	"main": "src/index.ts",
	"scripts": {
		"build": "tsc",
		"start": "node dist/index.js"
	},
	"dependencies": {
		"@aios/ipc": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.3.0"
	}
}

